// electron/ipc/carto.ts
//
// Cartografía — módulo reservado para handlers generales.
// Tras la poda de Fase 10, los handlers activos viven en `carto-graph.ts` y
// `carto-ai.ts`.

export function registerCartoHandlers(): void {
  // No-op.
}
