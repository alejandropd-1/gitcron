# Tasks

## 1. Fijación exacta en la instalación local (proceso principal)

- [x] 1.1 En `electron/pipeline/package-manager.ts`, `getPackageManagerInstallArgs` acepta un indicador opcional de exactitud y, en modo local, agrega `--save-exact` (pnpm, npm) o `--exact` (yarn, bun); sin el indicador los argumentos no cambian. Verificar con pruebas unitarias de los cuatro gestores, con y sin indicador, y que el modo global lo ignora.
- [x] 1.2 En `electron/pipeline/openspec-install.ts`, `installOpenSpecLocal` lee el `package.json` del repositorio antes de ejecutar y pide exactitud sólo si la entrada de `@fission-ai/openspec` (en `devDependencies` o `dependencies`) es una versión exacta sin prefijo; rango o ausencia → sin indicador. `commandExecuted` muestra el indicador. Verificar con pruebas que inyectan el lector de archivos: exacta `1.5.0` → comando con `--save-exact`; `^1.5.0` → sin indicador; sin entrada → sin indicador; `package.json` ilegible → sin indicador y la instalación sigue.

## 2. Verificar la versión pedida (dominio)

- [x] 2.1 En `components/pipeline/pipeline-domain.ts`, agregar al lado de `assessOpenSpecEngineAfterInstall` (sin cambiar su firma ni a sus consumidores) una evaluación que recibe el estado recalculado y la versión pedida: `ok` sólo si pasan las comprobaciones existentes **y** la versión que responde es igual a la pedida por comparación semver (`lib/openspec-version.ts`); si responde otra, `version-mismatch` con versión pedida, respondida y procedencia; `broken` y `unverified` como hoy. Verificar con pruebas unitarias de los cuatro veredictos, incluido el caso OdontoPau (pedida 1.13.2, responde 1.5.0 con procedencia `local`).

## 3. El recorrido combinado actualiza donde vive el motor

- [x] 3.1 `OpenSpecUpdateReview.tsx` pasa al runner la procedencia del motor resuelto (`status.cli.provenance`) junto con `installed` y `latest`. El texto del plan cambia según la procedencia: local → «actualizar el motor de este repositorio a v{{latest}}», global → el texto actual («…en toda la máquina…»), en `es`, `en` y `zh` de `lib/i18n.ts`. Verificar con pruebas del runner que muestran cada texto según la procedencia.
- [x] 3.2 En `OpenSpecUpdateRunner.tsx`, con procedencia `local` el paso del motor llama `installLocal({ repoPath, targetVersion: latest })` y nunca `installGlobal`; con `global`, `installGlobal({ repoPath, targetVersion: latest })`. Verificar actualizando `pipeline-openspec-update-runner.test.tsx`: la expectativa actual `toHaveBeenCalledWith({ repoPath: '/mock/repo' })` pasa a incluir `targetVersion`, y se agrega el caso local que comprueba que `installGlobal` no se llama.
- [x] 3.3 Con procedencia `managed` o `unknown`, el paso del motor no ofrece ejecutar: se declara no disponible con un texto que dice por qué (en los tres idiomas), y si había integración planeada, no corre en el mismo recorrido. Verificar con prueba del runner que no llama a ningún canal de instalación.
- [x] 3.4 El runner usa la evaluación de 2.1 con `targetVersion = latest`. Con `version-mismatch` el paso falla con un texto que dice versión pedida, versión que respondió y de dónde salió (local del repositorio / sistema), y la integración no corre (`stoppedAfterEngine`). El cartel «Listo: motor v…» no aparece. Verificar con una prueba que reproduce OdontoPau: instalación exitosa, estado recalculado con 1.5.0 local, pedida 1.13.2 → paso fallido, `runUpdate` sin llamar, sin «Listo».
- [x] 3.5 «Volver a la versión anterior» vuelve por el mismo canal por el que se instaló, con `targetVersion: installed`, y verifica con la evaluación de 2.1. Verificar con pruebas: tras actualizar local, volver atrás llama `installLocal` y no `installGlobal`; tras actualizar global, llama `installGlobal`.

## 4. Confirmación global dentro del recorrido

- [x] 4.1 Con procedencia `global`, tocar «Actualizar» no instala: muestra la confirmación global con comando, ruta del gestor y repositorios abiertos afectados (comando y gestor del canal `pipeline:openspec:install-plan`; repositorios de `openRepoPaths`, que `OpenSpecUpdateReview` ya recibe). Confirmar ejecuta **una sola** instalación y el recorrido sigue con la verificación de 3.4 y la integración; cancelar no invoca ningún gestor y deja el botón como estaba. Verificar con pruebas del runner: antes de confirmar, `installGlobal` no se llamó; después, se llamó una vez; al cancelar, cero veces. La advertencia de rama `main` / árbol sucio sigue apareciendo antes, como hoy.
- [x] 4.2 Con procedencia `local` no aparece la confirmación global (la instalación local es reversible con Git). Verificar con prueba del runner.

## 5. Integración

- [x] 5.1 Correr `pnpm verificar` en la rama del cambio y dejar todo en verde (build, pruebas ×2, tipos, eslint sobre lo tocado, `openspec validate --strict`, `git diff --check`, trampas).
- [ ] 5.2 Revisión en pantalla (la marca Alejandro): en OdontoPau, sin confirmar lo que quede, el plan dice «actualizar el motor de este repositorio a v1.13.2»; al correr, el `package.json` queda con `"@fission-ai/openspec": "1.13.2"` exacto y el lockfile modificados sin confirmar, el motor responde 1.13.2 y la integración escribe archivos. En gitCronos (motor del sistema) aparece la confirmación global antes de instalar. Descartar después en OdontoPau los cambios de prueba si Alejandro no quiere quedarse con la actualización.

## 6. La revisión dice la verdad después de actualizar (agregado tras la revisión en pantalla)

- [x] 6.1 Motivo real de «detenida» (design.md decisión 7): `generateUpdatePlan` (`electron/pipeline/openspec-preview.ts:157-167`) deja de poner el texto fijo de la POC; `deriveUpdateBlockReason` (`lib/openspec-update-guide.ts`) devuelve también `legacy-coexistence`, `customized`, `evidence-unknown` y `unclassified`; `OpenSpecUpdateReview.tsx` (`resolveBlockReasonText`) traduce el código (es/en/zh, en criollo, sin nombres internos) y no muestra `updatePlan.reason` ni el texto libre de `conflicts`. Verificar con pruebas: el estado medido de OdontoPau con copias viejas (skills `legacy-codex` + `new-agents`) da `legacy-coexistence` y la revisión no contiene «POC»; un estado bloqueado no clasificado muestra el texto de causa no determinada; ningún plan generado contiene el texto de la POC.
- [x] 6.2 La marca de carpeta compartida configura la herramienta (decisión 9): `inspectInstalledEvidence` (`electron/pipeline/openspec-evidence.ts`) lee `.agents/skills/.openspec-target` con la misma contención de rutas que el resto y agrega la herramienta nombrada a `configuredTools`; la regla de conflicto (`:420-426`) pasa a mirar skills de origen `legacy-codex`/`legacy-agent` en lugar de `configuredTools`. Verificar con pruebas sobre árboles temporales: (a) `.agents/skills` con marca `codex` + `.codex/config.toml` sin skills → Codex configurada, sin conflicto, y el estado del motor no queda `outdated` por eso; (b) lo mismo + `.codex/skills/openspec-explore/SKILL.md` → conflicto de convivencia y la copia informada como `legacy-codex`; (c) marca con un valor que no está en el registro → se ignora.
- [x] 6.3 Canales para retirar copias viejas (decisión 8): `pipeline:openspec:legacy-skills-plan` (sólo lectura: cada copia vieja con `removable` y, si no, `untracked` o `modified`) y `pipeline:openspec:remove-legacy-skills` (recalcula la lista en el proceso principal, no acepta rutas del renderer, borra sólo las retirables con `fs.rm` recursivo bajo `withRepoWatcherPaused`, no confirma, devuelve `{ removed, skipped, engineStatus }`). Preload y `types/electron.d.ts`. Verificar con pruebas sobre un repositorio Git temporal: copia seguida y limpia → se borra y queda como borrado sin confirmar en `git status`; copia no seguida → `skipped` con `untracked`; copia con cambios → `skipped` con `modified`; carga útil con claves de más → rechazo; repositorio no autorizado → rechazo.
- [x] 6.4 La revisión reemplaza la sección «Limpieza de configuración legacy (--force)» por «Copias viejas de las instrucciones» (textos es/en/zh en criollo): lista las retirables con «Retirar copias viejas» y una confirmación que enumera las carpetas; las no retirables con su motivo; al terminar, muestra lo borrado y el estado recalculado. La revisión ya no pasa `force` al recorrido; si el prop `force` de `OpenSpecUpdateRunner` queda sin uso, se saca junto con las claves `review.force*` que queden sin uso. Verificar con pruebas de la revisión: con copias viejas no aparece «--force»; confirmar llama al canal de retiro una vez; cancelar, cero; `runUpdate` se llama con `force` falso o sin él.
- [x] 6.5 Correr `pnpm verificar` en la rama y dejar todo en verde.
- [ ] 6.6 Revisión en pantalla (la marca Alejandro): en un repositorio con copias viejas en `.codex/skills` (se pueden reponer en OdontoPau con `git checkout HEAD -- .codex/skills` antes de confirmar el borrado), la cabecera dice la causa real, «Retirar copias viejas» las borra dejando los borrados sin confirmar, y después la integración queda al día sin volver a pedir «Actualizar».
