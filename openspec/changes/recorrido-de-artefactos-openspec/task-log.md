# Registro de tareas

- 2026-09-19 15:30 — persona — marcada — "1.5 Alejandro mira en el monitor de 32" y en el portátil y fija el valor del ancho. **La marca Alejandro.**"
- 2026-09-19 22:14 — persona — marcada — "1.6 **Decisión de Alejandro, 2026-09-19, mirando la tanda 1 en el monitor de 32": 1100 queda, pero sin umbral, y el reemplazo riel ↔ panel tiene que ser exacto.** Medido: con el panel derecho abierto el contenedor pierde 320 px, baja de 1400 y el ancho vuelve a 820 («se achica el contenido del medio»); el riel flotante mide 240 px y el panel derecho 320 (`PipelineWorkspace.tsx:63`, `rightWidth`), así que al reemplazarse el centro se corre 40 px; y las curvas difieren (`.switcherRail`: `300ms ease`; panel: `transition-all duration-300` con `cubic-bezier(0.4, 0, 0.2, 1)`, `RepoDetailsPanel.tsx:207`). Arreglo: (a) `--sdd-body-width: 1100px` sin container query; el cuerpo ya es `width: 100%`, así que en contenedores angostos ocupa lo que hay con su relleno y nunca salta entre dos números; (b) el riel flotante mide `rightWidth` (el dashboard ya recibe la prop y no la usa), publicado como `--right-panel-width`; (c) un solo token de movimiento (`--panel-motion: 300ms cubic-bezier(0.4, 0, 0.2, 1)`) que usan el riel y el panel; con mismo ancho y misma curva, el centro no se mueve al alternar."
- 2026-09-19 22:20 — persona — desmarcada — "1.6 **Decisión de Alejandro, 2026-09-19, mirando la tanda 1 en el monitor de 32": 1100 queda, pero sin umbral, y el reemplazo riel ↔ panel tiene que ser exacto.** Medido: con el panel derecho abierto el contenedor pierde 320 px, baja de 1400 y el ancho vuelve a 820 («se achica el contenido del medio»); el riel flotante mide 240 px y el panel derecho 320 (`PipelineWorkspace.tsx:63`, `rightWidth`), así que al reemplazarse el centro se corre 40 px; y las curvas difieren (`.switcherRail`: `300ms ease`; panel: `transition-all duration-300` con `cubic-bezier(0.4, 0, 0.2, 1)`, `RepoDetailsPanel.tsx:207`). Arreglo: (a) `--sdd-body-width: 1100px` sin container query; el cuerpo ya es `width: 100%`, así que en contenedores angostos ocupa lo que hay con su relleno y nunca salta entre dos números; (b) el riel flotante mide `rightWidth` (el dashboard ya recibe la prop y no la usa), publicado como `--right-panel-width`; (c) un solo token de movimiento (`--panel-motion: 300ms cubic-bezier(0.4, 0, 0.2, 1)`) que usan el riel y el panel; con mismo ancho y misma curva, el centro no se mueve al alternar."
- 2026-09-19 22:22 — persona — marcada — "1.6 **Decisión de Alejandro, 2026-09-19, mirando la tanda 1 en el monitor de 32": 1100 queda, pero sin umbral, y el reemplazo riel ↔ panel tiene que ser exacto.** Medido: con el panel derecho abierto el contenedor pierde 320 px, baja de 1400 y el ancho vuelve a 820 («se achica el contenido del medio»); el riel flotante mide 240 px y el panel derecho 320 (`PipelineWorkspace.tsx:63`, `rightWidth`), así que al reemplazarse el centro se corre 40 px; y las curvas difieren (`.switcherRail`: `300ms ease`; panel: `transition-all duration-300` con `cubic-bezier(0.4, 0, 0.2, 1)`, `RepoDetailsPanel.tsx:207`). Arreglo: (a) `--sdd-body-width: 1100px` sin container query; el cuerpo ya es `width: 100%`, así que en contenedores angostos ocupa lo que hay con su relleno y nunca salta entre dos números; (b) el riel flotante mide `rightWidth` (el dashboard ya recibe la prop y no la usa), publicado como `--right-panel-width`; (c) un solo token de movimiento (`--panel-motion: 300ms cubic-bezier(0.4, 0, 0.2, 1)`) que usan el riel y el panel; con mismo ancho y misma curva, el centro no se mueve al alternar."
- 2026-09-19 22:23 — persona — desmarcada — "1.6 **Decisión de Alejandro, 2026-09-19, mirando la tanda 1 en el monitor de 32": 1100 queda, pero sin umbral, y el reemplazo riel ↔ panel tiene que ser exacto.** Medido: con el panel derecho abierto el contenedor pierde 320 px, baja de 1400 y el ancho vuelve a 820 («se achica el contenido del medio»); el riel flotante mide 240 px y el panel derecho 320 (`PipelineWorkspace.tsx:63`, `rightWidth`), así que al reemplazarse el centro se corre 40 px; y las curvas difieren (`.switcherRail`: `300ms ease`; panel: `transition-all duration-300` con `cubic-bezier(0.4, 0, 0.2, 1)`, `RepoDetailsPanel.tsx:207`). Arreglo: (a) `--sdd-body-width: 1100px` sin container query; el cuerpo ya es `width: 100%`, así que en contenedores angostos ocupa lo que hay con su relleno y nunca salta entre dos números; (b) el riel flotante mide `rightWidth` (el dashboard ya recibe la prop y no la usa), publicado como `--right-panel-width`; (c) un solo token de movimiento (`--panel-motion: 300ms cubic-bezier(0.4, 0, 0.2, 1)`) que usan el riel y el panel; con mismo ancho y misma curva, el centro no se mueve al alternar."
- 2026-09-19 22:33 — persona — marcada — "2.3 Verificación real: con GitCron abierto sobre este repositorio, `openspec archive` de un change de sonda desde una terminal y un renombre de carpeta desde el Explorador funcionan. **La marca Alejandro.**"
- 2026-09-21 13:50 — persona — marcada — "3.5 Alejandro aprueba la forma de la línea temporal en pantalla. **La marca Alejandro.**"
- 2026-09-21 13:50 — persona — marcada — "3.6 **Decisión de Alejandro, 2026-09-21, mirando la tanda 3 en pantalla:** la línea temporal es horizontal (como Cronometric), no apilada; el círculo del nodo no lleva color por estado (el aro y el check verdes sobran: la palabra del estado alcanza); y el botón de cada nodo deja de arrastrar la clase de las solapas viejas `.pipeline-details__tab` (`OpenSpecDashboard.module.css:2822-2850`), que subraya en turquesa al pasar y deja el subrayado firme en la activa. Forma: fila de nodos unidos por una línea, nombre y estado debajo de cada uno, selección marcada en el nodo (aro y nombre en `--color-primary`); debajo de la fila, **una sola ficha** con descripción, ruta, acción o causa de bloqueo del nodo seleccionado (no cuatro fichas en fila: `2218586`). Las pruebas que exigían el sub-borde de la Observación 6 (`pipeline-sdd-body-layout.test.tsx:347-349`, `pipeline-details.test.tsx:47-54`) se retiran con causa: la tira de solapas ya no existe."
- 2026-09-21 13:50 — persona — marcada — "3.7 **Alejandro, 2026-09-21, mirando la tanda 3c:** cada nodo de la fila aparece con un fondo verde. Medido: lo pinta el bloque de la Observación 7 (`OpenSpecDashboard.module.css:2821-2899`, las fichas del grafo anterior: `li[data-state='done']` con fondo git-add al 18 % y el estado en verde), que la lista nueva hereda por conservar la clase `pipeline-artifact-graph` como gancho de pruebas. Se borra el bloque entero; los ganchos de clase quedan sin estilo. Además se revierte lo agregado por el ejecutor en 3c para una fixture: `description`/`outputPath` en `OpenSpecArtifactStatus` (`types/pipeline/index.ts`) y su lectura en `legacyGraph`; el parser de `status` (`openspec-cli.ts:170`) no los llena."
