## 1. Maquetación del cuerpo SDD para cualquier pantalla

- [x] 1.1 Declarar `--sdd-body-width` en la raíz del dashboard (`components/pipeline/OpenSpecDashboard.module.css`, `.dashboard` :72) con valor base 820 px y una container query hacia arriba (`@container (min-width: 1400px)`) que lo suba al valor que se decida en pantalla con Alejandro (punto de partida 1100 px). Todo bloque que hoy declara `max-width: 820px` (`.changeHeader` :234, `.center`/`.startBody` :1390, `.skeletonCenter` :4346) pasa a consumir la variable, con relleno lateral propio de `var(--space-5)` como mínimo.
- [x] 1.2 Que ningún panel del cuerpo se dibuje más ancho que el cuerpo: el panel «Confirmar archivado» (`OpenSpecDashboard.tsx` ~2599) y cualquier otro que hoy escape de la columna pasan a la misma variable. El encabezado pegajoso (`.changeHeader`) cubre el ancho del cuerpo con fondo opaco.
- [x] 1.3 Despegar el riel flotante del encabezado: `.switcherRail` (:2981) con `top` igual a la altura del encabezado más `var(--space-4)`, y el mismo espacio como margen superior en reposo. Sin tocar su ancho ni su plegado.
- [x] 1.4 Pruebas en `components/pipeline/__tests__/pipeline-sdd-body-layout.test.tsx` (hoy 21): encabezado, cuerpo, panel de archivado y esqueleto comparten el ancho; con contenedor de 1400 px o más el ancho es mayor que el base; el riel queda separado del encabezado. Sin bordes nuevos (escáner `commit-graph-frame.test.tsx`); escala visual (`lib/__tests__/visual-scale-scan.test.ts`).
- [x] 1.5 Alejandro mira en el monitor de 32" y en el portátil y fija el valor del ancho. **La marca Alejandro.**
- [x] 1.6 **Decisión de Alejandro, 2026-09-19, mirando la tanda 1 en el monitor de 32": 1100 queda, pero sin umbral, y el reemplazo riel ↔ panel tiene que ser exacto.** Medido: con el panel derecho abierto el contenedor pierde 320 px, baja de 1400 y el ancho vuelve a 820 («se achica el contenido del medio»); el riel flotante mide 240 px y el panel derecho 320 (`PipelineWorkspace.tsx:63`, `rightWidth`), así que al reemplazarse el centro se corre 40 px; y las curvas difieren (`.switcherRail`: `300ms ease`; panel: `transition-all duration-300` con `cubic-bezier(0.4, 0, 0.2, 1)`, `RepoDetailsPanel.tsx:207`). Arreglo: (a) `--sdd-body-width: 1100px` sin container query; el cuerpo ya es `width: 100%`, así que en contenedores angostos ocupa lo que hay con su relleno y nunca salta entre dos números; (b) el riel flotante mide `rightWidth` (el dashboard ya recibe la prop y no la usa), publicado como `--right-panel-width`; (c) un solo token de movimiento (`--panel-motion: 300ms cubic-bezier(0.4, 0, 0.2, 1)`) que usan el riel y el panel; con mismo ancho y misma curva, el centro no se mueve al alternar.

## 2. Un vigilante que no traba a nadie

- [x] 2.1 En `electron/ipc/watchers.ts`, reemplazar chokidar por un envoltorio propio sobre `fs.watch(root, { recursive: true, persistent: true })`: filtro `createRepoIgnoreFilter` sobre la ruta relativa de cada evento; estabilización de escrituras agrupando eventos del mismo archivo durante 200 ms; misma interfaz (`close()` y suscripción a eventos) que consumen `electron/ipc/git-ops.ts` y el pipeline. `withRepoWatcherPaused` sigue cerrando y rearmando. Sin dependencias nuevas; chokidar sale de `package.json` si no lo usa nadie más (medir con grep).
- [x] 2.2 Pruebas en `electron/__tests__/` (las de `watchers` y las de git-ops que consumen eventos, hoy en verde): un cambio en un archivo produce un evento; un archivo ignorado no; una ráfaga de escrituras del mismo archivo produce un solo evento estabilizado; y la prueba de sabotaje del renombre: con la observación activa sobre un repositorio temporal, renombrar una carpeta desde fuera **funciona** (con chokidar falla: pegar en el reporte la salida de la prueba sobre el código viejo).
- [x] 2.3 Verificación real: con GitCron abierto sobre este repositorio, `openspec archive` de un change de sonda desde una terminal y un renombre de carpeta desde el Explorador funcionan. **La marca Alejandro.**

## 3. El grafo de artefactos tal como lo devuelve el motor

- [x] 3.1 Canal IPC de sólo lectura que, para un cambio, devuelve la lista de artefactos con lo que trae `openspec instructions <artefacto> --change <id> --json` de cada uno (`description`, `dependencies`, `unlocks`, `resolvedOutputPath`, `existingOutputPaths`) y el estado de `openspec status --json`. Rutas validadas como el resto de los canales; sin escritura. Las dos superficies se suman a `consumedSurfaces` del análisis de versión (`electron/pipeline/openspec-version-analysis.ts:235-285`).
- [x] 3.2 Vista del grafo en `components/pipeline/`: una línea temporal con nodos unidos por una línea, ordenados por dependencias (un nodo después de los que lo desbloquean), con estado, descripción y ruta de salida por nodo. Mirar antes el intento rechazado (commit `2218586` de `change/remaquetar-cuerpo-de-sdd`, una fila de fichas) para no repetirlo. Ningún texto a mano; un campo ausente no se rellena; un motor que falla muestra el fallo.
- [x] 3.3 Desde un artefacto habilitado, disparar su operación con el lanzador existente (`PipelineRuntimeLauncher.tsx`), mostrando antes `existingOutputPaths`; un artefacto bloqueado no ofrece acción y muestra qué lo bloquea. Sin orden obligatorio.
- [x] 3.4 Pruebas contra JSON real del CLI 1.13 guardado como fixture (`electron/__tests__/`): la vista refleja el grafo devuelto y no uno derivado; bloqueado sin acción y con causa; fallo del motor sin grafo vacío; salida existente mostrada antes de disparar.
- [x] 3.5 Alejandro aprueba la forma de la línea temporal en pantalla. **La marca Alejandro.**
- [x] 3.6 **Decisión de Alejandro, 2026-09-21, mirando la tanda 3 en pantalla:** la línea temporal es horizontal (como Cronometric), no apilada; el círculo del nodo no lleva color por estado (el aro y el check verdes sobran: la palabra del estado alcanza); y el botón de cada nodo deja de arrastrar la clase de las solapas viejas `.pipeline-details__tab` (`OpenSpecDashboard.module.css:2822-2850`), que subraya en turquesa al pasar y deja el subrayado firme en la activa. Forma: fila de nodos unidos por una línea, nombre y estado debajo de cada uno, selección marcada en el nodo (aro y nombre en `--color-primary`); debajo de la fila, **una sola ficha** con descripción, ruta, acción o causa de bloqueo del nodo seleccionado (no cuatro fichas en fila: `2218586`). Las pruebas que exigían el sub-borde de la Observación 6 (`pipeline-sdd-body-layout.test.tsx:347-349`, `pipeline-details.test.tsx:47-54`) se retiran con causa: la tira de solapas ya no existe.
- [x] 3.7 **Alejandro, 2026-09-21, mirando la tanda 3c:** cada nodo de la fila aparece con un fondo verde. Medido: lo pinta el bloque de la Observación 7 (`OpenSpecDashboard.module.css:2821-2899`, las fichas del grafo anterior: `li[data-state='done']` con fondo git-add al 18 % y el estado en verde), que la lista nueva hereda por conservar la clase `pipeline-artifact-graph` como gancho de pruebas. Se borra el bloque entero; los ganchos de clase quedan sin estilo. Además se revierte lo agregado por el ejecutor en 3c para una fixture: `description`/`outputPath` en `OpenSpecArtifactStatus` (`types/pipeline/index.ts`) y su lectura en `legacyGraph`; el parser de `status` (`openspec-cli.ts:170`) no los llena.

## 4. La conversación que abre un cambio, con cara de aplicación

- [ ] 4.1 `components/pipeline/PipelineNewChangeFlow.tsx` pasa a un recorrido de pasos (explorar, proponer, aplicar, archivar): cada paso dice dónde está la persona, qué contestó el motor (la salida del runtime tal como llega, en el lugar que `explicar-el-ciclo-sin-tecnicismos` decida cómo redactar) y qué sigue. Un paso que el motor no expone se declara, no se simula. El lanzador sigue siendo el único que abre procesos; nada corre sin confirmación.
- [ ] 4.2 Pruebas en `components/pipeline/__tests__/` (hoy `pipeline-openspec-init-flow.test.tsx` 5 y las del flujo nuevo): un paso con salida del motor se marca hecho y presenta el siguiente; un paso no expuesto se declara; ninguna operación corre sin confirmar.
- [x] 4.3 Alejandro recorre la apertura de un cambio de sonda de punta a punta. **La marca Alejandro.**
- [ ] 4.4 **Medido el 2026-09-21 antes de la tanda 4a (CLI 1.13.1 sobre este repo):** `openspec instructions explore` → error `change_error` (el motor no expone explorar; se declara); proponer no tiene instrucción del motor porque el cambio no existe aún (la respuesta verificable es `openspec status --change <slug>` después de la sesión); `instructions apply --change <id> --json` devuelve `state`, `instruction`, `progress {total, complete, remaining}` y `tasks`; `instructions archive` devuelve sólo `context`, sin instrucción (GitCron archiva por copia). El lanzador descarta el `sessionId` que devuelve `pipelineRuntime.start` (`PipelineRuntimeLauncher.tsx:124-129`): `onStarted` pasa a recibirlo. El grupo se parte: **4a** recorrido con explorar y proponer completos y los pasos aplicar/archivar declarados en el riel (se hacen desde el cambio abierto, que el tablero selecciona solo al aparecer: `OpenSpecDashboard.tsx:1852-1856`); **4b**, si Alejandro lo aprueba al mirar 4a, el mismo recorrido montado sobre el cambio abierto con las respuestas del motor para aplicar y archivar.
- [ ] 4.5 **Regla deprecada con causa (auditoría de 4a, 2026-09-21):** `openspec/specs/pipeline-guided-workflow/spec.md:1805` exigía descartar el borrador al arrancar la sesión; el recorrido necesita que el borrador guarde la sesión de cada paso para mostrar qué contestó el motor. El ejecutor quitó el descarte sin declararlo y reescribió la prueba «arrancar la sesión lo descarta». Se conserva el cambio de regla y se declara en `specs/pipeline-guided-workflow/spec.md` de este change (MODIFIED): el borrador se descarta al cerrar el flujo y cuando el cambio propuesto aparece en el repositorio (`OpenSpecDashboard.tsx:1852-1856`, que hoy sólo cierra el flujo y deja el borrador: al reabrir «Nuevo cambio» aparece la propuesta anterior como hecha). Tanda 4a-bis: `closeFlow()` de ese punto pasa a `clearDraft(repoPath)` con prueba; `reasoningAvailable` derivado de `reasoningVisibility` de la sesión en vez de `null`; `onRefresh` fuera de las dependencias del efecto que consulta el grafo; CSS muerto del selector viejo (`.intentSwitch`, `.intentOption*`, `OpenSpecDashboard.module.css:1774-1812`) y su excepción en el escáner (`commit-graph-frame.test.tsx:186`).
- [ ] 4.6 **Alejandro recorre 4a el 2026-09-21 y el recorrido deja tres cosas al descubierto** (ningún runtime le anduvo: Claude con token revocado, OpenCode 1.15.10 con `ACP initialize response missing or protocol-incompatible` —defecto del adaptador, va aparte—, Codex muerto con `exit=1`). (a) Con Codex el motor de sesiones dijo `session.completed` y el recorrido ofreció «Continuar a proponer»: `runtime-session-hub.ts:300-310` sólo marca `failed` con `runtime.process.failed` o `run.completed success:false`; un `runtime.process.completed` con `exitCode ≠ 0` no cambia el desenlace. Se corrige: exit ≠ 0 sin `aborted` = `failed`. (b) El texto del error no se ve: `codex-normalizer.ts:43` lo reemplaza por `[REDACTED]` y `runtime-projection.ts:161` resume `runtime.error` sin el mensaje. Se muestra el mensaje tal como llega. (c) Las solapas de filtro de la bitácora (`ActivityFeed`, `.pipeline-filter` en `globals.css:910,3547`) llevan la estética vieja de píldoras; Alejandro la rechaza. Propuesta: selector segmentado como `.viewModeToggle` (`OpenSpecDashboard.module.css:3184`), en el recorrido y en Actividad, que son el mismo componente. La línea «Antigravity: agy version outside the reference baseline set» bajo el botón de arranque es a propósito (`PipelineRuntimeLauncher.tsx:311-319`): un runtime instalado que no se puede lanzar dice por qué.
- [ ] 4.7 **Alejandro recorre 4a-bis + 4c el 2026-09-21 y aparecen tres cosas.** (a) **Error de React en consola** al elegir el motor: «Cannot update a component (OpenSpecInspector) while rendering a different component (OpenSpecDashboard)». Medido: el bloque que detecta el cambio nuevo corre en el cuerpo del render (`OpenSpecDashboard.tsx:1847-1858`) y ahí escribe en dos stores externos —`setSelection` publica en `pipeline-store` (`:296-300`) y `clearDraft` en el store del borrador—. Derivar estado propio en render está bien; escribir en un store que otros componentes leen, no. `setSelection(added[0])` es **preexistente** (está en `main`, `0c4135c:1853`); `clearDraft` lo agregó la tanda 4a-bis. Arreglo: la derivación local queda en render y las dos escrituras externas pasan a un `useEffect` que depende de lo derivado. (b) **Los filtros de la bitácora quedaron mal con el selector segmentado**: son de selección múltiple y arrancan los cinco encendidos, así que se ven como un bloque turquesa que se va vaciando al desmarcar, y el hover no cambia el fondo. El segmentado es para elegir uno de varios: fue una mala indicación del auditor en la tanda 4c. Se rehace como control de encendido/apagado, sin fondo pleno por botón. (c) **Vocabulario**: «runtime» no se entiende. Alejandro pide textos en criollo («elegí el modelo de IA para avanzar»). El change `explicar-el-ciclo-sin-tecnicismos` (0 de 21 tareas) sigue siendo el dueño del vocabulario del panel entero; acá se deprecan del Non-Goal **sólo** las 66 claves `pipeline.launcher.*` y las `pipeline.journey.*`, que son las que la persona lee al recorrer la apertura, y se declara en design.md.
- [ ] 4.8 **Decisión de Alejandro, 2026-09-21: el número del ciclo SDD se sube en este change.** Medido: `OPENSPEC_CYCLE_TARGET_VERSION` sigue en `1.12.0` (`lib/openspec-version.ts:17`) mientras este change consumió superficies de 1.13 (`instructions` con `dependencies`/`unlocks`/`existingOutputPaths`, `status` con `artifacts`), y tres textos del análisis siguen diciendo 1.11.0 (`electron/pipeline/openspec-version-analysis.ts:383,391,411`). Subir la constante es un acto deliberado con evidencia (así lo declara `openspec-version-analysis.ts:24-25`): la evidencia es este change. Va en el cierre (grupo 6) con sus pruebas (`lib/__tests__/openspec-version.test.ts:42`, `electron/__tests__/openspec-version-analysis.test.ts:474`).

## 5. Decisión sobre el escáner de bordes

- [ ] 5.1 Medir y presentar: cuántas excepciones tiene hoy `allowedPatterns` en `components/__tests__/commit-graph-frame.test.tsx`, cuántas se agregaron por pantalla desde el 2026-09-07, y las tres salidas (seguir por lista; invertir el criterio prohibiendo el borde sólo en contenedores de maqueta; retirarlo). **La decide Alejandro**, y la decisión se escribe acá antes de tocar el archivo.
- [ ] 5.2 Aplicar la decisión escrita, con sus pruebas.

## 6. Cierre y validación

- [ ] 6.1 `pnpm build` en cero antes de las pruebas (`font-size-as-color` lee de `out/`).
- [ ] 6.2 `pnpm exec tsc --noEmit` sin errores de tipado; eslint sin avisos nuevos (los tres errores preexistentes de `set-state-in-effect` en `OpenSpecDashboard.tsx` y `OpenSpecInspector.tsx` no se arreglan ni se silencian en este cambio).
- [ ] 6.3 `pnpm test` en verde en dos pasadas consecutivas, informando «Test Files» y «Tests» de cada una.
- [ ] 6.4 `openspec validate recorrido-de-artefactos-openspec --strict` en cero; `git diff --check` en cero; `git status --short --branch` informado, sin confirmar nada en Git.
- [ ] 6.5 Revisión visual y funcional en la aplicación. **La marca Alejandro.**
